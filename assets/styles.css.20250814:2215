/* Reset and base styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    color: #333;
}

/* Header */
.header {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
}

.header h1 {
    color: white;
    font-size: 1.8rem;
    font-weight: 600;
}

.user-info {
    display: flex;
    align-items: center;
    gap: 1rem;
    color: white;
}

.user-info span {
    font-size: 0.9rem;
}

/* Buttons */
.btn {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    cursor: pointer;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
    text-align: center;
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
}

.btn-secondary {
    background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
}

.btn-secondary:hover {
    background: linear-gradient(135deg, #0984e3 0%, #74b9ff 100%);
}

/* NEW: Resign button styling */
.btn-danger {
    background: linear-gradient(135deg, #fd79a8 0%, #e84393 100%);
    border: 2px solid #e84393;
}

.btn-danger:hover {
    background: linear-gradient(135deg, #e84393 0%, #fd79a8 100%);
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(232, 67, 147, 0.3);
}

.btn-danger:disabled {
    background: linear-gradient(135deg, #ddd 0%, #bbb 100%);
    border-color: #bbb;
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
}

.btn-reset {
    background: transparent;
    color: #74b9ff;
    border: 1px solid #74b9ff;
    padding: 0.3rem 0.6rem;
    font-size: 0.8rem;
}

.btn-reset:hover {
    background: #74b9ff;
    color: white;
}

/* Container and layout */
.container {
    max-width: 1200px;
    margin: 2rem auto;
    padding: 0 1rem;
}

/* Authentication forms */
.auth-container {
    max-width: 400px;
    margin: 4rem auto;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.auth-container h2 {
    text-align: center;
    margin-bottom: 1.5rem;
    color: #333;
}

.form-group {
    margin-bottom: 1rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 500;
    color: #555;
}

.form-group input,
.form-group select {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid #e0e0e0;
    border-radius: 6px;
    font-size: 1rem;
    transition: border-color 0.3s ease;
}

.form-group input:focus,
.form-group select:focus {
    outline: none;
    border-color: #667eea;
}

.form-helper {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 0.5rem;
}

.form-helper small {
    color: #777;
    font-size: 0.8rem;
}

/* Lobby */
.lobby {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
}

.panel {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.panel h2 {
    margin-bottom: 1.5rem;
    color: #333;
    font-size: 1.3rem;
}

/* Game list */
.game-list {
    max-height: 400px;
    overflow-y: auto;
}

.game-item.your-turn {
    border-left: 4px solid #00b894;
    background: linear-gradient(135deg, rgba(0, 184, 148, 0.1) 0%, rgba(0, 184, 148, 0.05) 100%);
}

.game-item.your-turn .game-info h3::after {
    content: " 🔔";
    font-size: 0.8em;
}

.game-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    margin-bottom: 0.5rem;
    background: rgba(255, 255, 255, 0.7);
    border-radius: 8px;
    border: 1px solid rgba(0, 0, 0, 0.1);
}

.game-item:hover {
    background: rgba(255, 255, 255, 0.9);
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.game-info h3 {
    margin-bottom: 0.25rem;
    color: #333;
    font-size: 1rem;
}

.game-info p {
    color: #666;
    font-size: 0.9rem;
}

/* Game area */
.game-area {
    display: grid;
    grid-template-columns: 300px 1fr;
    gap: 2rem;
    max-width: 1400px;
    margin: 2rem auto;
    padding: 0 1rem;
}

.game-sidebar {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    padding: 1.5rem;
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    height: fit-content;
}

.game-main {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Game status */
.game-status {
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
}

.game-status h3 {
    margin-bottom: 0.5rem;
    color: #333;
}

.game-status p {
    color: #666;
    font-size: 0.9rem;
}

/* NEW: Check warning styling */
.check-warning {
    margin-top: 1rem;
    padding: 0.75rem;
    background: linear-gradient(135deg, #ff7675 0%, #d63031 100%);
    color: white;
    border-radius: 6px;
    text-align: center;
    font-weight: 500;
    animation: pulse-warning 2s infinite;
}

@keyframes pulse-warning {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
}

/* Player list */
.player-list {
    margin-bottom: 1.5rem;
}

.player-list h3 {
    margin-bottom: 1rem;
    color: #333;
}

.player-item {
    padding: 0.75rem;
    margin-bottom: 0.5rem;
    background: rgba(255, 255, 255, 0.7);
    border-radius: 6px;
    border-left: 4px solid #ddd;
    transition: all 0.3s ease;
}

.player-item.current {
    border-left-color: #667eea;
    background: rgba(102, 126, 234, 0.1);
    font-weight: 500;
}

.player-item:hover {
    background: rgba(255, 255, 255, 0.9);
    transform: translateX(2px);
}

/* NEW: Game controls section */
.game-controls {
    margin-top: 1.5rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(0, 0, 0, 0.1);
}

/* Hexagonal board */
/* Hexagonal board - proper tessellation */
/* Hexagonal Chess Board - Proper Tessellation */
/* Hexagonal Chess Board - Proper Tessellation */

.hex-board {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 20px;
  /* Optional: Add some padding to prevent edge clipping */
}

.hex-row {
  position: relative;
  display: flex;
  flex-direction: row;
  /* Vertical spacing: Full height minus half height for overlap */
  margin-top: -17.5px; /* -(35px / 2) for 1/2 height overlap */
}

.hex-row:first-child {
  margin-top: 0; /* No overlap for the first row */
}

/* Offset every other row horizontally by half hex width */
.hex-row:nth-child(even) {
  margin-left: 20px; /* 40px / 2 = 20px offset */
}

.hex-cell {
  width: 40px;
  height: 35px;
  position: relative;
  margin: 0; /* Remove any default margins */
  /* Optional: Add small negative margin to eliminate tiny gaps */
  margin-right: -1px;
}

.hex-content {
  width: 100%;
  height: 100%;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  /* Ensure content is centered within each hex */
}

/* Optional: Visual debugging - remove these in production */
/*
.hex-cell {
  border: 1px solid rgba(255, 0, 0, 0.3);
}
.hex-row:nth-child(even) .hex-cell {
  border-color: rgba(0, 0, 255, 0.3);
}
*/



/* Optional: Visual debugging - remove these in production */
/*
.hex-cell {
  border: 1px solid rgba(255, 0, 0, 0.3);
}
.hex-row:nth-child(even) .hex-cell {
  border-color: rgba(0, 0, 255, 0.3);
}
*/

.hex-board {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
    padding: 2rem;
}

.hex-row {
    display: flex;
    gap: 20px;
    margin-top: -20px; /* Overlap rows by 3/4 height for proper tessellation */
}

.hex-row:first-child {
    margin-top: 0;
}

.hex-row:nth-child(even) {
    margin-left: 0px; /* Offset even rows by half hex width */
}

.hex-cell {
    width: 40px;
    height: 35px;
    position: relative;
    cursor: pointer;
    transition: all 0.2s ease;
    margin-right: -2px; /* Slight overlap for seamless appearance */
}

.hex-cell:hover {
    transform: scale(1.05);
    z-index: 10;
}

.hex-content {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    clip-path: polygon(25% 0%, 75% 0%, 100% 50%, 75% 100%, 25% 100%, 0% 50%);
    font-size: 1.2rem;
    font-weight: bold;
    border: 2px solid #333;
    transition: all 0.3s ease;
    position: relative;
    z-index: 1;
}

/* Hex cell colors */
.pastel-red .hex-content {
    background: linear-gradient(135deg, #fab1a0 0%, #e17055 100%);
}

.pastel-green .hex-content {
    background: linear-gradient(135deg, #81ecec 0%, #00b894 100%);
}

.pastel-blue .hex-content {
    background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
}

/* Game state classes */
.hex-cell.selected .hex-content {
    background: linear-gradient(135deg, #00b894 0%, #00a085 100%) !important;
    border-color: #00a085;
    box-shadow: 0 0 15px rgba(0, 184, 148, 0.6);
    transform: scale(1.1);
    z-index: 20;
}

.hex-cell.valid-move .hex-content {
    border-color: #fdcb6e;
    box-shadow: 0 0 10px rgba(253, 203, 110, 0.7);
    animation: pulse 1.5s infinite;
}

.hex-cell.capture-square .hex-content {
    border-color: #e17055;
    box-shadow: 0 0 10px rgba(225, 112, 85, 0.7);
    animation: pulse-red 1.5s infinite;
}

/* King in check highlighting */
.hex-cell.king-in-check .hex-content {
    border-color: #d63031 !important;
    background: linear-gradient(135deg, #ff7675 0%, #d63031 100%) !important;
    box-shadow: 0 0 20px rgba(214, 48, 49, 0.8) !important;
    animation: pulse-check 1s infinite !important;
    transform: scale(1.1) !important;
    z-index: 25;
}

@keyframes pulse-check {
    0%, 100% { 
        box-shadow: 0 0 20px rgba(214, 48, 49, 0.8);
        transform: scale(1.1);
    }
    50% { 
        box-shadow: 0 0 30px rgba(214, 48, 49, 1);
        transform: scale(1.15);
    }
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.6; }
}

@keyframes pulse-red {
    0%, 100% { 
        opacity: 1;
        border-color: #e17055;
    }
    50% { 
        opacity: 0.7;
        border-color: #d63031;
    }
}

/* Pieces */
.piece {
    color: white;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
    transition: transform 0.2s ease;
    position: relative;
    z-index: 5;
}

.red-piece {
    color: #e17055;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
}

.blue-piece {
    color: #0984e3;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
}

.green-piece {
    color: #00b894;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
}

.piece:hover {
    transform: scale(1.2);
}

/* Responsive design */
@media (max-width: 768px) {
    .hex-cell {
        width: 35px;
        height: 30px;
    }
    
    .hex-content {
        font-size: 1rem;
    }
    
    .hex-row:nth-child(even) {
        margin-left: 17px; /* Adjust offset for smaller hexes */
    }
    
    .hex-row {
        margin-top: -12px; /* Adjust vertical overlap for smaller hexes */
    }
}

@media (max-width: 480px) {
    .hex-cell {
        width: 30px;
        height: 26px;
    }
    
    .hex-content {
        font-size: 0.9rem;
    }
    
    .hex-row:nth-child(even) {
        margin-left: 15px;
    }
    
    .hex-row {
        margin-top: -10px;
    }
}


@keyframes pulse-check {
    0%, 100% { 
        box-shadow: 0 0 20px rgba(214, 48, 49, 0.8);
        transform: scale(1.1);
    }
    50% { 
        box-shadow: 0 0 30px rgba(214, 48, 49, 1);
        transform: scale(1.15);
    }
}

/* Demo page specific styles */
.piece-nav-btn {
    background: rgba(255, 255, 255, 0.8);
    border: 2px solid #ddd;
    padding: 0.5rem;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 0.9rem;
    font-weight: 500;
}

.piece-nav-btn:hover {
    background: rgba(255, 255, 255, 1);
    border-color: #667eea;
    transform: translateY(-2px);
}

.piece-nav-btn.active {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-color: #764ba2;
}

.piece-desc {
    display: none;
    padding: 1rem;
    background: rgba(255, 255, 255, 0.7);
    border-radius: 6px;
    margin-top: 1rem;
}

.piece-desc.active {
    display: block;
}

.piece-desc h4 {
    margin-bottom: 0.5rem;
    color: #333;
}

.piece-desc p {
    color: #666;
    font-size: 0.9rem;
    line-height: 1.4;
}

/* Notifications */
.notification {
    position: fixed;
    top: 20px;
    right: 20px;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    padding: 1rem 1.5rem;
    border-radius: 8px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    border-left: 4px solid #667eea;
    min-width: 300px;
    max-width: 400px;
    z-index: 1000;
    transform: translateX(100%);
    opacity: 0;
    transition: all 0.3s ease;
}

.notification.show {
    transform: translateX(0);
    opacity: 1;
}

.notification.error {
    border-left-color: #e17055;
}

.notification.success {
    border-left-color: #00b894;
}

.notification.warning {
    border-left-color: #fdcb6e;
}

.notification.info {
    border-left-color: #74b9ff;
}

.notification-title {
    font-weight: 600;
    margin-bottom: 0.25rem;
    color: #333;
}

.notification-message {
    color: #666;
    font-size: 0.9rem;
}

/* NEW: AI thinking indicator */
.ai-thinking {
    display: inline-block;
    background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.9rem;
    margin-top: 0.5rem;
    animation: pulse-ai 1.5s infinite;
}

@keyframes pulse-ai {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.8; transform: scale(1.02); }
}

.game-mode-option {
    background: rgba(255, 255, 255, 0.1);
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-radius: 8px;
    padding: 1rem;
    margin-bottom: 1rem;
    transition: all 0.3s ease;
}

.game-mode-option:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
}

.ai-difficulty-easy { border-left-color: #00b894; }
.ai-difficulty-medium { border-left-color: #fdcb6e; }
.ai-difficulty-hard { border-left-color: #e17055; }

/* Responsive design */
@media (max-width: 768px) {
    .header {
        padding: 1rem;
        flex-direction: column;
        gap: 1rem;
    }
    
    .lobby {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .game-area {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .game-sidebar {
        order: 2;
    }
    
    .game-main {
        order: 1;
        padding: 1rem;
    }
    
    .hex-cell {
        width: 35px;
        height: 30px;
    }
    
    .hex-content {
        font-size: 1rem;
    }
    
    .notification {
        right: 10px;
        left: 10px;
        min-width: auto;
        max-width: none;
    }
}

@media (max-width: 480px) {
    .container {
        padding: 0 0.5rem;
    }
    
    .auth-container {
        margin: 2rem auto;
        padding: 1.5rem;
    }
    
    .panel {
        padding: 1.5rem;
    }
    
    .hex-cell {
        width: 30px;
        height: 26px;
    }
    
    .hex-content {
        font-size: 0.9rem;
    }
    
    .user-info {
        flex-direction: column;
        gap: 0.5rem;
        text-align: center;
    }
}