* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    min-height: 100vh;
}

.header {
    background: rgba(0, 0, 0, 0.2);
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    backdrop-filter: blur(10px);
}

.header h1 {
    font-size: 24px;
    background: linear-gradient(45deg, #fff, #e0e0e0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.user-info {
    display: flex;
    gap: 1rem;
    align-items: center;
}

.btn {
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 6px;
    cursor: pointer;
    font-size: 14px;
    font-weight: 600;
    transition: all 0.3s ease;
}

.btn:hover {
    transform: translateY(-1px);
    filter: brightness(1.1);
}

.btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
}

.btn-reset {
    background: rgba(255, 193, 7, 0.2);
    border: 1px solid rgba(255, 193, 7, 0.5);
    color: #ffc107;
    font-size: 12px;
    padding: 4px 8px;
    margin-left: 8px;
}

.btn-reset:hover {
    background: rgba(255, 193, 7, 0.3);
    color: #fff;
}

.container {
    max-width: 1200px;
    margin: 2rem auto;
    padding: 0 2rem;
}

.auth-container {
    max-width: 400px;
    margin: 4rem auto;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    border-radius: 12px;
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.form-group {
    margin-bottom: 1rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
}

.form-group input, .form-group select {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.1);
    color: white;
    font-size: 14px;
}

.form-group input::placeholder {
    color: rgba(255, 255, 255, 0.6);
}

.form-group input[name="game_name"] {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
    cursor: text;
}

.form-group input[name="game_name"]:focus {
    border-color: #667eea;
    outline: none;
    box-shadow: 0 0 0 2px rgba(102, 126, 234, 0.3);
}

.form-helper {
    font-size: 12px;
    color: rgba(255, 255, 255, 0.7);
    margin-top: 5px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.lobby {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
}

.panel {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    border-radius: 12px;
    padding: 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.panel h2 {
    margin-bottom: 1rem;
    font-size: 18px;
}

.game-list {
    max-height: 400px;
    overflow-y: auto;
}

.game-item {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    padding: 1rem;
    margin-bottom: 0.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.game-info h3 {
    font-size: 16px;
    margin-bottom: 0.25rem;
}

.game-info p {
    font-size: 12px;
    opacity: 0.8;
}

.game-area {
    display: flex;
    height: calc(100vh - 120px);
}

.game-sidebar {
    width: 300px;
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(20px);
    border-right: 1px solid rgba(255, 255, 255, 0.2);
    padding: 20px;
    display: flex;
    flex-direction: column;
}

.game-main {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
}

.player-list {
    margin-bottom: 1rem;
}

.player-item {
    padding: 0.5rem;
    margin-bottom: 0.25rem;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.05);
}

.player-item.current {
    background: rgba(46, 204, 113, 0.2);
    border: 1px solid #2ecc71;
}

.game-status {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 15px;
    margin-bottom: 20px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    text-align: center;
}

.hex-board {
    display: flex;
    flex-direction: column;
    align-items: center;
    transform: scale(0.8);
    margin: 20px auto;
}

.hex-row {
    display: flex;
    margin: -6px 0;
    position: relative;
    justify-content: center;
}

.hex-row:nth-child(even) {
    margin-left: 2px;
}

.hex-cell {
    width: 52px;
    height: 60px;
    margin: 0 2px;
    position: relative;
    cursor: pointer;
    transition: all 0.15s ease;
}

.hex-content {
    width: 100%;
    height: 100%;
    position: relative;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
}

.hex-cell.pastel-red .hex-content {
    background: linear-gradient(135deg, #ffb3ba, #ff9999);
}

.hex-cell.pastel-blue .hex-content {
    background: linear-gradient(135deg, #bae1ff, #87ceeb);
}

.hex-cell.pastel-green .hex-content {
    background: linear-gradient(135deg, #baffc9, #90ee90);
}

.hex-cell:hover {
    transform: scale(1.05);
    z-index: 10;
}

.hex-cell.selected {
    transform: scale(1.1);
    z-index: 20;
}

.hex-cell.selected .hex-content {
    border: 3px solid #2ecc71;
    background: rgba(46, 204, 113, 0.2) !important;
}

.hex-cell.king-in-check {
    animation: checkPulse 1.5s infinite;
    z-index: 25;
}

.hex-cell.king-in-check .hex-content {
    border: 4px solid #e74c3c !important;
    background: rgba(231, 76, 60, 0.3) !important;
}

@keyframes checkPulse {
    0%, 100% { 
        transform: scale(1.1);
        opacity: 1;
    }
    50% { 
        transform: scale(1.25);
        opacity: 0.7;
    }
}

.hex-cell.valid-move {
    animation: pulseMove 1.2s infinite;
    z-index: 15;
}

.hex-cell.valid-move .hex-content {
    border: 4px solid #2ecc71 !important;
    background: rgba(46, 204, 113, 0.4) !important;
}

@keyframes pulseMove {
    0%, 100% { 
        transform: scale(1.0);
        opacity: 1;
    }
    50% { 
        transform: scale(1.15);
        opacity: 0.8;
    }
}

/* Fix the conflicting .hex-content rules */

/* This creates the hexagon shape and background */
.hex-content {
    width: 100%;
    height: 100%;
    position: relative;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
}

.tile {
  position: relative;
  width: 90px; height: 90px;   /* whatever your hex’s content box is */
}
.tile {
  display: grid;
  place-items: center;     /* centers child */
  width: 90px; height: 90px;
}
.piece { display: block; }

/* Add this CSS to your styles.css to fix mobile pawn glitch */

/* Ensure consistent piece styling across all devices */
.piece {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 5;
    pointer-events: none;
    font-size: 48px;
    font-weight: 900;
    line-height: 1;
    text-shadow: 
        -1px -1px 0 #000,
        1px -1px 0 #000,
        -1px 1px 0 #000,
        1px 1px 0 #000;
    filter: none;
    transition: all 0.2s ease;
    user-select: none;
    opacity: 1;
    
    /* Force consistent font rendering on mobile */
    font-family: "Segoe UI Symbol", "Noto Color Emoji", "Apple Color Emoji", sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

/* Add missing black piece styling */
.piece.black-piece {
    color: #2c3e50; /* Dark blue-gray instead of pure black for better visibility */
}

/* Ensure consistent piece colors across all players */
.piece.red-piece {
    color: #dc143c;
}

.piece.blue-piece {
    color: #1e90ff;
}

.piece.green-piece {
    color: #228b22;
}

.piece.yellow-piece {
    color: #f1c40f;
}

.piece.purple-piece {
    color: #9b59b6;
}

.piece.orange-piece {
    color: #e67e22;
}

/* Mobile-specific fixes */
@media (max-width: 768px) {
    .piece {
        /* Slightly larger on mobile for better visibility */
        font-size: 52px;
        
        /* Stronger text shadow for mobile screens */
        text-shadow: 
            -2px -2px 0 #000,
            2px -2px 0 #000,
            -2px 2px 0 #000,
            2px 2px 0 #000,
            0 0 4px rgba(0, 0, 0, 0.5);
    }
    
    /* Ensure pieces don't get cut off on smaller hexagons */
    .hex-cell {
        width: 48px;
        height: 56px;
    }
    
    /* Adjust hex board scaling for mobile */
    .hex-board {
        transform: scale(0.7);
    }
}

/* Force consistent Unicode characters - add this if you're using specific piece characters */
.piece[data-piece="king"]::before { content: "♔"; }
.piece[data-piece="queen"]::before { content: "♕"; }
.piece[data-piece="rook"]::before { content: "♖"; }
.piece[data-piece="bishop"]::before { content: "♗"; }
.piece[data-piece="knight"]::before { content: "♘"; }
.piece[data-piece="pawn"]::before { content: "♙"; }

/* Alternative: If you want filled pieces for black */
.piece.black-piece[data-piece="king"]::before { content: "♚"; }
.piece.black-piece[data-piece="queen"]::before { content: "♛"; }
.piece.black-piece[data-piece="rook"]::before { content: "♜"; }
.piece.black-piece[data-piece="bishop"]::before { content: "♝"; }
.piece.black-piece[data-piece="knight"]::before { content: "♞"; }
.piece.black-piece[data-piece="pawn"]::before { content: "♟"; }

/* Ensure pieces display correctly on high-DPI mobile screens */
@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
    .piece {
        /* Better rendering on retina displays */
        text-rendering: optimizeLegibility;
        -webkit-font-feature-settings: "liga" 1;
        font-feature-settings: "liga" 1;
    }
}
/* end pawn specification piece code */


/* This positions the chess pieces inside the hexagons */
.piece {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 5;
    pointer-events: none;
    font-size: 48px;
    font-weight: 900;
    line-height: 1;
    text-shadow: 
        -1px -1px 0 #000,
        1px -1px 0 #000,
        -1px 1px 0 #000,
        1px 1px 0 #000;
    filter: none;
    transition: all 0.2s ease;
    user-select: none;
    opacity: 1;
}

/* Keep your existing piece color classes */
.piece.red-piece {
    color: #dc143c;
}

.piece.blue-piece {
    color: #1e90ff;
}

.piece.green-piece {
    color: #228b22;
}


.notification {
    position: fixed;
    top: 20px;
    right: 20px;
    background: rgba(0, 0, 0, 0.9);
    color: white;
    padding: 15px 20px;
    border-radius: 8px;
    border-left: 4px solid #e74c3c;
    backdrop-filter: blur(10px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    z-index: 1000;
    transform: translateX(400px);
    transition: transform 0.3s ease;
    max-width: 300px;
}

.notification.show {
    transform: translateX(0);
}

.notification.success {
    border-left-color: #2ecc71;
}

.notification.warning {
    border-left-color: #f39c12;
}

.notification.error {
    border-left-color: #e74c3c;
}

.notification-title {
    font-weight: bold;
    margin-bottom: 5px;
}

.notification-message {
    font-size: 12px;
    opacity: 0.9;
}

.piece-nav-btn {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
    padding: 8px 4px;
    border-radius: 6px;
    cursor: pointer;
    font-size: 11px;
    font-weight: 600;
    transition: all 0.3s ease;
    text-align: center;
}

.piece-nav-btn:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: translateY(-1px);
}

.piece-nav-btn.active {
    background: linear-gradient(135deg, #667eea, #764ba2);
    border-color: #667eea;
    transform: translateY(-1px);
}

.piece-desc {
    display: none;
    font-size: 13px;
    line-height: 1.4;
    margin-bottom: 15px;
}

.piece-desc.active {
    display: block;
}

.piece-desc h4 {
    color: #2ecc71;
    margin-bottom: 8px;
    font-size: 14px;
}

.hex-cell.capture-square {
    z-index: 16;
}

.hex-cell.capture-square .hex-content {
    border: 4px solid #e74c3c !important;
    background: rgba(231, 76, 60, 0.4) !important;
    animation: pulseCapture 1.2s infinite;
}

@keyframes pulseCapture {
    0%, 100% { 
        transform: scale(1.0);
        opacity: 1;
    }
    50% { 
        transform: scale(1.15);
        opacity: 0.8;
    }
}

/* Add this CSS to your demo page for better spacing */

.player-list h3 {
    margin-bottom: 15px; /* Add space below "Choose Piece Type" */
}

/* Optional: Additional improvements for demo page */
.piece-nav-btn {
    margin-bottom: 8px; /* Space between piece buttons */
}

.piece-desc {
    margin-top: 20px; /* Space above piece descriptions */
    padding: 15px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    border-left: 3px solid #667eea;
}

.piece-desc.active {
    background: rgba(102, 126, 234, 0.1);
    border-left-color: #2ecc71;
}

/* Mobile-Responsive UI Redesign - Add to your styles.css */

/* Enhanced mobile-first responsive design */
@media (max-width: 768px) {
    
    /* Header adjustments for mobile */
    .header {
        padding: 0.5rem 1rem;
        flex-direction: column;
        gap: 0.5rem;
        text-align: center;
    }
    
    .header h1 {
        font-size: 20px;
    }
    
    .user-info {
        gap: 0.5rem;
        flex-wrap: wrap;
        justify-content: center;
    }
    
    .user-info .btn {
        padding: 6px 12px;
        font-size: 12px;
    }
    
    /* Container adjustments */
    .container {
        margin: 1rem auto;
        padding: 0 1rem;
    }
    
    /* Lobby layout - stack vertically on mobile */
    .lobby {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .panel {
        padding: 1rem;
    }
    
    .panel h2 {
        font-size: 16px;
    }
    
    /* GAME AREA - Complete mobile redesign */
    .game-area {
        flex-direction: column;
        height: auto;
        min-height: 100vh;
    }
    
    /* Move sidebar to top on mobile */
    .game-sidebar {
        width: 100%;
        order: 1;
        background: rgba(255, 255, 255, 0.08);
        backdrop-filter: blur(20px);
        border-right: none;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        padding: 15px;
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        gap: 10px;
        justify-content: space-between;
    }
    
    /* Game board gets priority space */
    .game-main {
        order: 2;
        flex: 1;
        padding: 10px;
        min-height: 60vh;
    }
    
    /* Compact game status */
    .game-status {
        flex: 1;
        min-width: 200px;
        padding: 10px;
        margin-bottom: 0;
        text-align: left;
    }
    
    .game-status h3 {
        font-size: 14px;
        margin-bottom: 4px;
    }
    
    .game-status p {
        font-size: 12px;
    }
    
    /* Collapsible player list */
    .player-list {
        flex: 1;
        min-width: 150px;
        margin-bottom: 0;
    }
    
    .player-list h3 {
        font-size: 14px;
        margin-bottom: 8px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }
    
    .player-list h3::after {
        content: "▼";
        font-size: 12px;
        transition: transform 0.3s ease;
    }
    
    .player-list.collapsed h3::after {
        transform: rotate(-90deg);
    }
    
    .player-list.collapsed #playerList {
        display: none;
    }
    
    .player-item {
        padding: 6px;
        margin-bottom: 4px;
        font-size: 12px;
    }
    
    /* Compact game controls */
    .game-controls {
        width: 100%;
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
    }
    
    .game-controls .btn {
        flex: 1;
        min-width: 100px;
        padding: 8px 12px;
        font-size: 12px;
        margin-bottom: 0 !important;
    }
    
    /* Enhanced hex board for mobile */
    .hex-board {
        transform: scale(0.75);
        margin: 10px auto;
    }
    
    /* Larger touch targets for mobile */
    .hex-cell {
        width: 56px;
        height: 64px;
        margin: 0 3px;
    }
    
    /* Larger pieces for better visibility */
    .piece {
        font-size: 54px;
        text-shadow: 
            -2px -2px 0 #000,
            2px -2px 0 #000,
            -2px 2px 0 #000,
            2px 2px 0 #000,
            0 0 6px rgba(0, 0, 0, 0.7);
    }
    
    /* Mobile form improvements */
    .form-group input, 
    .form-group select {
        padding: 12px;
        font-size: 16px; /* Prevents zoom on iOS */
        border-radius: 8px;
    }
    
    .form-group label {
        font-size: 14px;
    }
    
    .btn {
        padding: 12px 16px;
        font-size: 14px;
        min-height: 44px; /* iOS recommended touch target */
    }
    
    /* Enhanced check warning for mobile */
    .check-warning {
        background: rgba(231, 76, 60, 0.2);
        border: 2px solid #e74c3c;
        border-radius: 8px;
        padding: 8px;
        margin: 8px 0;
        font-size: 12px;
        text-align: center;
        animation: warningPulse 2s infinite;
    }
    
    @keyframes warningPulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.7; }
    }
    
    /* Mobile notification adjustments */
    .notification {
        top: 10px;
        right: 10px;
        left: 10px;
        max-width: none;
        transform: translateY(-100px);
    }
    
    .notification.show {
        transform: translateY(0);
    }
    
    /* Demo page mobile improvements */
    .piece-nav-btn {
        padding: 10px 6px;
        font-size: 12px;
        margin-bottom: 6px;
    }
    
    .piece-desc {
        font-size: 12px;
        padding: 12px;
    }
    
    /* Auth container mobile adjustments */
    .auth-container {
        margin: 2rem auto;
        padding: 1.5rem;
    }
    
    /* Game list mobile improvements */
    .game-list {
        max-height: 300px;
    }
    
    .game-item {
        flex-direction: column;
        align-items: flex-start;
        gap: 8px;
    }
    
    .game-info h3 {
        font-size: 14px;
    }
    
    .game-info p {
        font-size: 11px;
    }
}

/* Portrait orientation specific adjustments */
@media (max-width: 768px) and (orientation: portrait) {
    .game-main {
        min-height: 50vh;
    }
    
    .hex-board {
        transform: scale(0.7);
    }
}

/* Landscape mobile adjustments */
@media (max-width: 768px) and (orientation: landscape) {
    .game-area {
        flex-direction: row;
    }
    
    .game-sidebar {
        width: 250px;
        order: 1;
        flex-direction: column;
        border-right: 1px solid rgba(255, 255, 255, 0.2);
        border-bottom: none;
    }
    
    .game-main {
        order: 2;
    }
    
    .hex-board {
        transform: scale(0.8);
    }
}

/* Very small screens (iPhone SE, etc.) */
@media (max-width: 375px) {
    .header {
        padding: 0.5rem;
    }
    
    .container {
        padding: 0 0.5rem;
    }
    
    .hex-board {
        transform: scale(0.6);
    }
    
    .hex-cell {
        width: 48px;
        height: 56px;
    }
    
    .piece {
        font-size: 48px;
    }
    
    .game-sidebar {
        padding: 10px;
        gap: 8px;
    }
    
    .game-controls .btn {
        min-width: 80px;
        padding: 6px 10px;
        font-size: 11px;
    }
}

.timer-settings {
    background: rgba(255, 255, 255, 0.05);
    border: 2px dashed rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    padding: 15px;
    margin-top: 10px;
    transition: all 0.3s ease;
}

.timer-settings.active {
    background: rgba(102, 126, 234, 0.1);
    border-color: rgba(102, 126, 234, 0.3);
}

.timer-info {
    color: rgba(255, 255, 255, 0.8);
    font-size: 14px;
    margin-bottom: 10px;
    text-align: center;
}

.form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
}

@media (max-width: 768px) {
    .form-row {
        grid-template-columns: 1fr;
    }
}

/* Add JavaScript functionality for collapsible player list */
/* Add this script tag to your index.php */